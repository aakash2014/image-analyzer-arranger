# Core — always required
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pillow>=10.0.0
imagehash>=4.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
python-multipart>=0.0.5
aiofiles>=23.0.0

# InsightFace (face detection + recognition).
# Optional — falls back to OpenCV Haar cascade if missing.
# On Windows, you may need: pip install insightface
# Requires C++ build tools: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# insightface>=0.7.0

# Phase 2+ dependencies (not needed for MVP, install when upgrading):
# onnxruntime>=1.15.0
# onnx>=1.14.0
# transformers>=4.30.0
# torch>=2.0.0
